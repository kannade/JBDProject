# JBDProject

Приложение на Spring Boot для трекинга учебных задач с JWT-аутентификацией и управлением пользователями, задачами и
группами задач.

---

## 🐳️ Запуск приложения

1. Склонируйте проект.
2. Настройки БД в `application.yml` (все уже настроено, менять не нужно).
3. Существует возможность запустить весь проект в docker (пункт 3.1) и запуск для разработки (пункт 3.2: в docker
   запускается только БД, проект запускаем через IDE).

#### 3.1 Для продуктивного запуска приложения:

Запуск:

```bash
docker-compose up -d
```

Проверка:

```bash
docker-compose ps
```

#### 3.2 Для разработки:

Запуск:

```bash
docker-compose up -d db pgadmin
```

Запустите Spring Boot:

```bash
./gradlew bootRun
```

или через IDE (Run `DemoApplication`).

Приложение доступно на `http://localhost:8080`.

Swagger UI: `http://localhost:8080/swagger-ui/index.html`

---

## 🔄 Порядок использования

1. Зарегистрироваться через `/api/auth/register`.
2. Получить JWT через `/api/auth/login`.
3. Передавать JWT в заголовке `Authorization: Bearer <token>` для всех защищённых эндпоинтов.
4. Для Swagger жмем кнопку Authorize и вставляем туда тело самого токена. После этого Swagger автоматически подставляет
   токен в заголовок Authorization для всех защищённых эндпоинтов.
5. Создавать и управлять своими группами задач через `/api/groups`.
6. Создавать и управлять своими задачами через `/api/tasks`.
7. Администраторы могут просматривать всех пользователей, задачи и группы через `/api/admin`.
---

## 🔑 Основные эндпоинты

### 1. Регистрация и авторизация (AuthController)

| Метод | URL                  | Описание                        |
|-------|----------------------|---------------------------------|
| POST  | `/api/auth/register` | Регистрация нового пользователя |
| POST  | `/api/auth/login`    | Авторизация, возвращает JWT     |

---

### 2. Пользователи (UserController)

| Метод | URL                  | Описание                                  |
|-------|----------------------|-------------------------------------------|
| POST  | `/api/users`         | Создать нового пользователя (НЕ АКТУАЛЕН) |
| GET   | `/api/users/{email}` | Получить пользователя по email            |
| GET   | `/api/users`         | Список всех пользователей                 |
| GET   | `/api/users/hello`   | Тестовый эндпоинт                |

---

### 3. Задачи (TaskController)

| Метод  | URL                      | Описание                    |
|--------|--------------------------|-----------------------------|
| GET    | `/api/tasks`             | Получить список своих задач |
| POST   | `/api/tasks`             | Создать новую задачу        |
| PUT    | `/api/tasks/{id}`        | Обновить задачу             |
| DELETE | `/api/tasks/{id}`        | Удалить задачу              |
| PATCH  | `/api/tasks/{id}/status` | Изменить статус задачи      |
| PATCH  | `/api/tasks/{id}/group`  | Изменить группу задачи      |

---

### 4. Группы задач (TaskGroupController)

| Метод  | URL                | Описание                          |
|--------|--------------------|-----------------------------------|
| GET    | `/api/groups`      | Получить список своих групп задач |
| POST   | `/api/groups`      | Создать новую группу              |
| PUT    | `/api/groups/{id}` | Переименовать группу              |
| DELETE | `/api/groups/{id}` | Удалить группу                    |

---

### 5. Админские функции (AdminController)

| Метод | URL                                  | Описание                        |
|-------|--------------------------------------|---------------------------------|
| GET   | `/api/admin/users`                   | Список всех пользователей       |
| GET   | `/api/admin/tasks`                   | Список всех задач               |
| GET   | `/api/admin/groups`                  | Список всех групп задач         |
| GET   | `/api/admin/users/{userId}/tasks`    | Задачи конкретного пользователя |
| GET   | `/api/admin/tasks/statistics/status` | Статистика задач по статусам    |
| GET   | `/api/admin/tasks/statistics/group`  | Статистика задач по группам     |
| GET   | `/api/admin/users/search`            | Поиск пользователей по имени (пример SQL-запроса)     |
